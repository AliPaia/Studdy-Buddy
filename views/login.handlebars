<header class='container text-center'>
  <header class='container text-left'></header>
  <header class='container text-right'></header>

  <form>
    <div>
      <label>Username:</label>
      <input id='user-create' type='text' name='name' />
      <label>Password</label>
      <input id='password-create' type='password' name='password' />
      <button id='create-account' class='btn btn-primary btn-grad'>Create
        Account</button>
    </div>

    <div>
      <label>Username:</label>
      <input id='username' type='text' name='name' />
      <label>Password</label>
      <input id='password' type='password' name='password' />
      <button id='profile-login' class='btn btn-primary btn-grad'>Go to Profile</button>
    </div>
    {{!    duplicate username/password to the line below and keep "create account" button on the top line and move "go to profile button to the bottom page"
                when login is clicked the button should change to logout
                when create account is clicked user should be routed to homepage page
                when go to profile is clicked user should be routed to assessment page }}
    {{! <input class='checkbox' type='checkbox' value='Remember Me' />
        <input class="linkspara"><a href="#" class="links">Forgot Password? }}
  </form>
  <header class='container text-left'></header>
  <header class='container text-right'></header>
</header>

{{#section 'script'}}
  <script src='/js/login.js'></script>
{{/section}}